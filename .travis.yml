sudo: false
language: python
cache:
  directories:
  - ${HOME}/.cache
script:
  - pip install tox
  - tox
notifications:
  email: false
env:
  matrix:
    - TOXENV=flake8-py3
    - TOXENV=py26
    - TOXENV=py27
    - TOXENV=py33
    - TOXENV=py34

